{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "RepoStatus",
  "description": "Represents the observed state of a repository.",
  "type": "object",
  "properties": {
    "originCommitId": {
      "type": "string",
      "description": "Last commit identifier of the origin repo."
    },
    "targetCommitId": {
      "type": "string",
      "description": "Last commit identifier of the target repo."
    },
    "targetBranch": {
      "type": "string",
      "description": "Branch where commit was done in the target repo."
    },
    "originBranch": {
      "type": "string",
      "description": "Branch where commit was done in the origin repo."
    }
  },
  "additionalProperties": false
}
